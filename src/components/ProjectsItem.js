import React from "react"

const ProjectsItem = props => {
  return (
    <div className={`projects__image icon--search`}>
      <img src="" alt={props.imageAlt} />
    </div>
  )
}

export default ProjectsItem
